# Hands-on Exercise 4 Implementation Plan

**Goal:** Create a locally rendered English student submission for Chapter 8, Spatial Weights and Applications, in the existing ISSS626 Quarto website.

**Scope:** The user's latest instruction excludes Exercise 3. Use the supplied local Chapter 8 HTML and `Geo/data.zip`. Preserve existing exercises and website styling. Do not commit, push, or deploy.

**Architecture:** A self-contained `Hands-on_Ex/Hands-on_Ex04/Hands-on_Ex04.qmd` uses its own `data/aspatial` and `data/geospatial` files. Add one navbar entry in `_quarto.yml`. Retain the professor's analysis order and examples, adapting tmap calls to installed version 4 where necessary.

**Tech stack:** R 4.6.1, Quarto 1.10.18, sf, spdep, tmap, tidyverse, knitr.

- [x] Read the supplied Chapter 8 material and inspect existing QMD/YAML conventions.
- [x] Verify the archive contains 88 Hunan polygons and 88 rows with 29 development fields; extract into the exercise folder.
- [x] Install the missing spdep dependency and validate data joins and neighbour structures.
- [x] Write the QMD: overview; data and packages; import and join; GDPPC maps; Queen/Rook neighbours and plots; 62 km and six-neighbour weights; inverse-distance weights; row standardisation; four spatial lag/window applications; reference.
- [x] Include short original English explanations and computed answers to the professor's questions. Clarify that `style = "B"` with supplied inverse-distance weights is not row standardisation, and that entry 10 (not entry 1) illustrates eight neighbours.
- [x] Add the Exercise 4 navbar entry and render the page with actual R execution.
- [x] Verify output figures, join completeness, neighbourhood counts, row sums, and all four lag/window formulas against direct neighbour calculations.
- [x] Refresh local navigation outputs, serve a localhost preview, inspect figures and menu links, and record the final local-only status.

## Verification record

- `quarto render Hands-on_Ex/Hands-on_Ex04/Hands-on_Ex04.qmd`: exit 0 with all 39 R chunks executed, including data and numerical assertions.
- `quarto render --use-freezer --no-clean`: exit 0 for all 12 project documents. Existing computations were reused to refresh navigation; existing QMD sources were not edited.
- All 11 new figure files load in the browser; both comparison tables contain 88 rows; no browser console errors or horizontal page overflow.
- All 11 website pages include the new Exercise 4 navbar link. The existing twelfth document is a Reveal.js slide deck without the website navbar, by design. All page and menu targets return HTTP 200 locally.
- All seven supplied data files are byte-identical to `data.zip`.
- The exercise uses Queen 448 links, Rook 440 links, a 62 km graph with 324 links, and six neighbours per county for KNN. All four lag/window results match independent direct calculations on the neighbour lists.
- Local preview: http://127.0.0.1:8765/Hands-on_Ex/Hands-on_Ex04/Hands-on_Ex04.html
- No Exercise 3 was created. No staging, commit, push, or deployment was performed.
